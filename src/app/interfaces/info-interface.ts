export interface Infopage {
    titulo?: string,
    email?: string,
    pagina_autor?: string,
    facebook?: string,
    instagram?: string,
    whatsapp?: number,
    equipo?: string []
}//el signo de interrogacion es porque es opcional y para que no me muestre error al importar en info-page.service